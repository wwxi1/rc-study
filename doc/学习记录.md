# 王韦翔 8.6

##  8.6 Git  基础操作

- 学习了GIT和github的基础使用方法，了解了git存储文件改动的方法。
- 在github上创建了自己的库，同步了项目文件（中途多次挂着梯子上传不上，发现是网络不稳定，开热点解决）
- 复习C语言的语法
- 进一步了解STM32中的各种名称如 AHB,APB，RCC，HCLK,PCLK,DMA等
- 了解FLASH与CSS的含义与作用



1. **git init**：本地初始化Git仓库

2. 关联github远程仓库

3. **git add** ：把当前文件夹改过的文件加入暂存区

4. **git commit -m “ xxxxxx”**  :把暂存区的文件打包成一个本地版本快照,标注xxxxxxx，要做好标注且必须做标注，否则无法提交。

5. **git checkout -b xxxxxxxxx**:新建一个名为xxxxx的分支
6. 改后重复3,4，然后g**it checkout main**：切回主分支
7. **git log --oneline**  :以精简的单行格式展示当前分支的提交历史,可以从这里看到哈希值。可以加参数限定查看范围，比如   **git log --oneline -n 5**   只显示最近5条提交记录。（--online 作用是使单行显示）

8. **git diff main xxxxxx**: 比较main分支与xxxxx分支的区别

9. **git merge xxxxx**：把xxxx分支的改动整合到当前分支上（一般是主分支）

10. **push**：推送到远程仓库

- **pull** 从远程仓库拉取内容上





| 代码                   |         作用         |              |
| :--------------------- | :------------------: | :----------: |
| git commit -m 'xxxxxx' |      提交并标注      |   提交时用   |
| git checkout xxxxx     |     切换到该分支     | 切换分支时用 |
| git checkout -b xxxxxx |     新建xxxx分支     | 新建分支时用 |
| git log                | 显示改动日志与哈希值 | 找哈希值时用 |
| git merge xxxxx        |         整合         |   整合时用   |





```
git checkout main
git log --oneline
git diff main feature-blink
git merge feature-blink
```

​	切换到main分支，简洁显示改动日志，主要是用来看改动标注和哈希值，然后比较main分支与 feature-binik分支的区别，最后整合到main分支


https://github.com/wwxi1/rc-study